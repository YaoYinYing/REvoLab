services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: revolab
      POSTGRES_USER: revolab
      POSTGRES_PASSWORD: revolab
    ports:
      - "5432:5432"
    volumes:
      - revolab-postgres:/var/lib/postgresql/data

volumes:
  revolab-postgres:
